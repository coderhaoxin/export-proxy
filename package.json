{
  "name": "export-proxy",
  "version": "0.1.0",
  "description": "export proxy",
  "main": "build/index.js",
  "scripts": {
    "test-cov": "istanbul cover node_modules/.bin/_mocha -- --require babel-core/register -R dot -t 2000 test/*.js",
    "test": "mocha --require babel-core/register",
    "build": "babel lib --out-dir build",
    "prepublish": "npm run build"
  },
  "repository": "coderhaoxin/export-proxy",
  "keywords": [
    "export",
    "proxy"
  ],
  "files": [
    "build/",
    "lib/"
  ],
  "author": "haoxin",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "6",
    "babel-core": "6",
    "babel-preset-es2015": "6",
    "istanbul": "0",
    "mocha": "2"
  }
}
